Emacs Todo

* Todo
  - [ ] In dired, entering a number and hitting return should go to the line you
    entered. For example:
    - User types "11"
    - User then hits RET
    - Point should jump to line 11
    - The only action should be point jumping
      - Add an option that causes the command to open the file at point or if
        the target is a directory, visit the directory at the target (replacing
        the current dired buffer)
